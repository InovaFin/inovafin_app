<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Estado Normal -->
    <item android:state_focused="false" android:state_pressed="false">
        <shape android:shape="rectangle">
            <solid android:color="@color/branco_background" /> <!-- Cor de fundo no estado normal -->
            <stroke android:width="1dp" android:color="@color/verde_principal" /> <!-- Cor da borda -->
            <corners android:radius="5dp"/>
        </shape>
    </item>

    <!-- Estado Focado -->
    <item android:state_focused="true" android:state_pressed="false">
        <shape android:shape="rectangle">
            <solid android:color="#E9FFF1" /> <!-- Cor de fundo quando focado -->
            <stroke android:width="1dp" android:color="@color/verde_principal" /> <!-- Cor da borda quando focado -->
            <corners android:radius="5dp"/>
        </shape>
    </item>
</selector>
